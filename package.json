{
  "name": "kicktipp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "chromedriver": "^76.0.1",
    "dotenv": "^4.0.0",
    "nightwatch": "^0.9.21"
  },
  "scripts": {
    "build": "tsc",
    "nightwatch": "nightwatch",
    "start": "yarn build && yarn run nightwatch",
    "docker": "yarn build && docker build ."
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/nightwatch": "^1.1.1",
    "@types/node": "^12.7.3",
    "typescript": "^3.6.2"
  }
}
